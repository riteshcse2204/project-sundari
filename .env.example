PORT=4174
NODE_ENV=production

# Copy this file to .env for local PostgreSQL.
# DATABASE_URL=postgresql://sundari_user:strong_password@localhost:5432/sundari_care
# For local PostgreSQL without SSL, keep this false. Hosted DBs usually need SSL.
DATABASE_SSL=false

# Replace this before production use.
SESSION_SECRET=change-this-long-random-secret
